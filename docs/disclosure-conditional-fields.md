<!-- GENERATED by export-docs.mjs — do not edit.
     Source: rules/disclosure.js -->

# Conditional fields in the change-of-address form

> A field is shown only once the answers it depends on have been given. Answers already entered are kept when a field is withdrawn, and are restored unchanged if it reappears.

## Rationale

A form that shows everything at once asks people about circumstances that do not apply to them, and a form that shows things one at a time hides how long it is going to be. The compromise is to reveal only what follows from an answer already given.

The retention half matters more than the reveal half. Someone who picks "married", enters their partner, then corrects the entry to "single" has made a correction — not a request to delete data. Discarding it silently punishes the correction, and in a setting where the form is evidence, an unlogged deletion is worse than a redundant field.

## Edge cases

### An answer is corrected and a field is withdrawn

The withdrawn field keeps its content. It is not submitted while hidden, and it is restored unchanged if the field reappears. Nothing is deleted without the person doing it.

### A withdrawn field held a mandatory entry

The requirement is withdrawn with the field. A hidden field must never block submission — the person cannot see what is being asked of them.

### Chained dependencies

Child blocks depend on the number, which depends on whether there are children at all. Withdrawing the top of the chain withdraws everything below it in one step, not one level per interaction.

### Reducing the number of children

Blocks are withdrawn from the end. Reducing from three to two withdraws the third block — never the one most recently edited, and never renumbered so entries appear to shift between children.

### A field appears after the person has scrolled past it

New fields appear in place, without scrolling the page. Moving the viewport under someone loses their position and reads as a page reload.

### Gender given as diverse or not specified

No salutation is derived from it. The form addresses everyone by full name throughout, so that an entry outside the two conventional options does not produce a different-looking form for that person.

## Open questions

These are not oversights. They are what the specification leaves undecided —
recorded so they can be answered rather than quietly assumed.

- How long are withdrawn answers kept — for the session only, or across a saved draft? The second needs a retention period and someone to set it.
- Above how many children does a list of blocks stop working and become a table? This is the threshold rule again, on a different quantity.
- Which EU member states count as "EU" for the residence-permit condition, and who maintains that list when it changes?
- Is the church-tax question mandatory, and what is recorded when someone declines to answer — "none" or "not stated"? Those are not the same.
- Does a partner who is not moving still have to be named, or only the fact that they exist?

## Conditions

| Field | Shown when | Reads |
|---|---|---|
| `partnerDetails` — Partner — name and whether they are moving too | maritalStatus is 'married' or 'civil partnership' | `maritalStatus` |
| `formerName` — Name before marriage | maritalStatus is 'divorced' or 'widowed' | `maritalStatus` |
| `residencePermit` — Residence permit number | citizenship is not 'German' and not an EU member state | `citizenship` |
| `childCount` — Number of children moving with you | hasChildren is 'yes' | `hasChildren` |
| `childDetails` — One block per child | childCount is 1 or more | `childCount` |

Always present: `gender`, `maritalStatus`, `citizenship`, `hasChildren`, `churchTax`

## Provenance

| Date | Change | By | Basis |
|---|---|---|---|
| 2026-08-19 | Rule created — reveal conditions and retention on withdrawal | Example — the deciding person would be named here | Synthetic example, created to test this format on a nested case |

## Logic

```js
function visibleFields(answers) {
    return CONDITIONS.filter((c) => c.test(answers)).map((c) => c.field);
  }
```
